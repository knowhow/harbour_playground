(function(a,b)  { 
    
     button.text = (a * b) * 2; 
     button.text = "a * b * 2 = " + button.text;

     button.styleSheet = 'color:red ; font: bold 14px Georgia, serif;';
     button.show();

});

